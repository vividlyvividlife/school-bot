# Mini App - Варианты размещения

## 🎯 Вариант 1: Встроенный веб-сервер (Самый простой)

Бот сам отдает HTML-файлы через встроенный веб-сервер.

### Шаг 1: Запуск бота с веб-сервером

```bash
python bot_with_webapp.py
```

Вы увидите:
```
📱 Mini App server started at http://0.0.0.0:8080
🤖 Bot started
💡 Use ngrok to make it public: ngrok http 8080
```

### Шаг 2: Сделать доступным через интернет

**Вариант А: ngrok (для тестирования)**

1. Установите ngrok: https://ngrok.com/download
2. Запустите:
```bash
ngrok http 8080
```
3. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
4. Обновите `config.py`:
```python
WEBAPP_URL = 'https://abc123.ngrok.io'
```

**Вариант Б: Публичный сервер**

Если у вас есть сервер с публичным IP:
1. Откройте порт 8080 в файрволе
2. Используйте ваш IP или домен
3. Обновите `config.py`:
```python
WEBAPP_URL = 'http://ваш-ip:8080'
```

⚠️ **Важно**: Telegram требует HTTPS для Mini App! Используйте:
- ngrok (автоматически дает HTTPS)
- Nginx с SSL сертификатом
- Cloudflare Tunnel

---

## 🌐 Вариант 2: Бесплатный хостинг

### GitHub Pages (Рекомендуется для статики)

1. Создайте репозиторий на GitHub
2. Загрузите папку `webapp`
3. Включите GitHub Pages в настройках
4. URL будет: `https://username.github.io/repo-name/`

**Настройка:**
```bash
cd webapp
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/username/repo-name.git
git push -u origin main
```

В настройках репозитория: Settings → Pages → Source: main branch

### Netlify

1. Зарегистрируйтесь на https://netlify.com
2. Перетащите папку `webapp` в Netlify
3. Получите URL: `https://your-app.netlify.app`

### Vercel

1. Зарегистрируйтесь на https://vercel.com
2. Установите Vercel CLI:
```bash
npm install -g vercel
```
3. Деплой:
```bash
cd webapp
vercel
```

---

## 🔧 Вариант 3: Telegram Bot API (Экспериментально)

Telegram позволяет отдавать HTML напрямую через Bot API, но это ограничено.

**Ограничения:**
- Только один HTML файл
- Нет внешних CSS/JS (все inline)
- Размер файла до 20 МБ

Для нашего проекта это **не подходит**, так как у нас несколько JS файлов.

---

## 🚀 Рекомендация

### Для разработки/тестирования:
✅ **Встроенный веб-сервер + ngrok**
- Быстро запускается
- Не нужен внешний хостинг
- Идеально для локальной разработки

### Для продакшн:
✅ **GitHub Pages или Netlify**
- Бесплатно
- Автоматический HTTPS
- CDN для быстрой загрузки
- Не нужно держать сервер

---

## 📝 Пошаговая инструкция (ngrok)

1. **Установите ngrok:**
   - Windows: Скачайте с https://ngrok.com/download
   - Mac: `brew install ngrok`
   - Linux: `snap install ngrok`

2. **Зарегистрируйтесь и получите токен:**
   ```bash
   ngrok authtoken YOUR_TOKEN
   ```

3. **Запустите бот с веб-сервером:**
   ```bash
   python bot_with_webapp.py
   ```

4. **В другом терминале запустите ngrok:**
   ```bash
   ngrok http 8080
   ```

5. **Скопируйте HTTPS URL из ngrok:**
   ```
   Forwarding  https://abc123.ngrok.io -> http://localhost:8080
   ```

6. **Обновите config.py:**
   ```python
   WEBAPP_URL = 'https://abc123.ngrok.io'
   ```

7. **Перезапустите бота**

8. **Откройте бота в Telegram и нажмите "🌐 Открыть таблицу"**

---

## 🔒 Безопасность

### Для встроенного веб-сервера:

1. **Используйте HTTPS** (ngrok или SSL сертификат)
2. **Ограничьте доступ** только для Telegram:
   ```python
   # В webapp_server.py добавьте проверку
   TELEGRAM_IPS = ['149.154.160.0/20', '91.108.4.0/22']
   ```
3. **Не храните секреты** в JavaScript

### Для GitHub Pages/Netlify:

1. **Не коммитьте** `.env` файл
2. **Используйте environment variables** для чувствительных данных
3. **Валидируйте данные** на стороне бота

---

## 🧪 Тестирование

После настройки проверьте:

1. ✅ Mini App открывается в Telegram
2. ✅ Загружаются CSS и JS файлы
3. ✅ Определяется роль пользователя
4. ✅ Отображаются данные из бота

---

## ❓ FAQ

**Q: Нужен ли мне сервер?**
A: Нет, если используете GitHub Pages или Netlify.

**Q: Можно ли использовать без интернета?**
A: Нет, Mini App требует HTTPS URL.

**Q: Ngrok URL меняется при перезапуске?**
A: Да, бесплатная версия дает случайный URL. Платная версия позволяет зафиксировать.

**Q: Как обновить Mini App?**
A: Просто обновите файлы в `webapp/` и перезапустите сервер или сделайте новый деплой.

---

**Готово!** Выберите подходящий вариант и наслаждайтесь Mini App! 🎉
