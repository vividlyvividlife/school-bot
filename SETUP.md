# Инструкция по настройке School Bot

## Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например, "School Manager Bot")
4. Введите username бота (например, "school_manager_bot")
5. Скопируйте полученный **токен** бота

## Шаг 2: Настройка окружения

1. Создайте файл `.env` в корне проекта:
```bash
cp .env.example .env
```

2. Откройте `.env` и вставьте токен бота:
```
BOT_TOKEN=ваш_токен_здесь
WEBAPP_URL=https://your-webapp-url.com
```

## Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

Или установите вручную:
```bash
pip install aiogram==3.4.1 aiohttp==3.9.1 python-dotenv==1.0.0
```

## Шаг 4: Настройка Mini App (опционально)

Для полноценной работы Mini App вам нужно:

### Вариант 1: Локальное тестирование
1. Установите локальный веб-сервер:
```bash
cd webapp
python -m http.server 8000
```

2. Используйте ngrok для создания публичного URL:
```bash
ngrok http 8000
```

3. Скопируйте HTTPS URL из ngrok и обновите `WEBAPP_URL` в `.env`

### Вариант 2: Деплой на хостинг
1. Загрузите папку `webapp` на любой хостинг (GitHub Pages, Netlify, Vercel и т.д.)
2. Обновите `WEBAPP_URL` в `.env` на URL вашего хостинга

### Вариант 3: Без Mini App
Если не хотите использовать Mini App, просто закомментируйте строки с `WebAppInfo` в `keyboards.py`

## Шаг 5: Запуск бота

```bash
python bot.py
```

Вы должны увидеть:
```
INFO - Bot started
```

## Шаг 6: Первое использование

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Введите ваше ФИО
4. Вы автоматически получите роль **Учителя** (первый пользователь)

## Основные команды

### Для учителя:
- `/add_student` - Добавить ученика
- `/add_subject` - Добавить предмет
- Используйте кнопки меню для остальных функций

### Для родителя:
- `/link_child` - Привязать ребенка

### Общие:
- `/start` - Главное меню
- `/help` - Справка

## Структура базы данных

База данных SQLite создается автоматически при первом запуске в файле `school_bot.db`

## Troubleshooting

### Ошибка "Invalid token"
- Проверьте правильность токена в `.env`
- Убедитесь, что токен не содержит пробелов

### Ошибка "Module not found"
- Установите зависимости: `pip install -r requirements.txt`

### Mini App не открывается
- Проверьте `WEBAPP_URL` в `config.py`
- Убедитесь, что URL доступен по HTTPS
- Проверьте, что веб-сервер запущен

### База данных не создается
- Проверьте права на запись в директорию
- Убедитесь, что путь `DATABASE_PATH` корректен

## Дополнительные настройки

### Изменение системы оценок
В `config.py` измените:
```python
MIN_GRADE = 1
MAX_GRADE = 10  # Можно изменить на 5 или 12
```

### Настройка уведомлений о дедлайнах
В `config.py`:
```python
DEADLINE_REMINDER_DAYS = 1  # За сколько дней напоминать
```

## Безопасность

- ⚠️ **Никогда** не публикуйте файл `.env` с токеном!
- ⚠️ Добавьте `.env` в `.gitignore` (уже сделано)
- ⚠️ Регулярно делайте бэкапы базы данных `school_bot.db`

## Бэкап базы данных

Создайте копию базы данных:
```bash
cp school_bot.db school_bot_backup_$(date +%Y%m%d).db
```

Или автоматизируйте через cron (Linux/Mac):
```bash
0 0 * * * cp /path/to/school_bot.db /path/to/backups/school_bot_$(date +\%Y\%m\%d).db
```

## Обновление бота

1. Остановите бота (Ctrl+C)
2. Обновите код
3. Запустите снова: `python bot.py`

База данных сохранится автоматически.

## Поддержка

Если возникли проблемы:
1. Проверьте логи в файле `bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте версию Python (требуется 3.10+)

---

**Готово!** Ваш School Bot настроен и готов к использованию! 🎉
