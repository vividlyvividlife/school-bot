# 🚀 Настройка связки Mini App (GitHub Pages) → VDS API

## 📋 Что есть

- **VDS**: `45.147.178.113`
- **Mini App**: Размещен на GitHub Pages
- **База данных**: SQLite на VDS с тестовыми данными

## 🎯 Что нужно сделать

### 1️⃣ Настроить CORS на VDS сервере

Откройте файл `webapp_server.py` и добавьте CORS middleware:

```python
from aiohttp import web
from aiohttp_cors import setup as cors_setup, ResourceOptions

def create_webapp_server(host='0.0.0.0', port=8080):
    """Создание веб-сервера для Mini App"""
    app = web.Application()
    
    # Настройка CORS для доступа с GitHub Pages
    cors = cors_setup(app, defaults={
        "*": ResourceOptions(
            allow_credentials=True,
            expose_headers="*",
            allow_headers="*",
            allow_methods="*"
        )
    })
    
    # Static routes
    app.router.add_get('/miniapp', serve_index)
    # ... остальные роуты
    
    # Apply CORS to all routes
    for route in list(app.router.routes()):
        cors.add(route)
    
    return app, host, port
```

### 2️⃣ Установить aiohttp-cors на VDS

```bash
pip install aiohttp-cors
```

### 3️⃣ Добавить API_URL в index.html

Добавьте в `webapp/index.html` перед закрывающим тегом `</head>`:

```html
<!-- API Configuration -->
<script>
    // URL вашего VDS сервера где запущен бот с API
    const API_URL = 'http://45.147.178.113:8080';
</script>
```

### 4️⃣ Обновить api.js

Замените метод `getBaseUrl()` в `webapp/js/api.js`:

```javascript
const API = {
    // Получение базового URL для API
    getBaseUrl() {
        // Если задана переменная API_URL - используем её
        if (typeof API_URL !== 'undefined') {
            console.log('Using API_URL:', API_URL);
            return API_URL;
        }
        
        //По умолчанию используем текущий origin
        console.log('Using current origin:', window.location.origin);
        return window.location.origin;
    },
    // ... остальной код
```

### 5️⃣ Запустить бота на VDS

На VDS выполните:

```bash
# Перейдите в каталог проекта
cd /path/to/chatbotSchool

# Убедитесь что база заполнена
python add_demo_data.py

# Запустите бота с веб-сервером
python bot_with_webapp.py
```

Бот должен запуститься на порту 8080 и быть доступен по адресу `http://45.147.178.113:8080`

### 6️⃣ Залить изменения на GitHub

```bash
git add .
git commit -m "Add API_URL configuration for VDS connection"
git push origin main
```

GitHub Actions автоматически задеплоит изменения на GitHub Pages.

## 🧪 Проверка работы

### 1. Проверьте что API доступен

Откройте в браузере: `http://45.147.178.113:8080/api/students`

Должен вернуться JSON с списком учеников.

### 2. Откройте Mini App в Telegram

Бот должен открыть Mini App с GitHub Pages, который будет делать запросы к VDS API.

### 3. Проверьте консоль браузера

Нажмите F12 в Telegram и посмотрите:
- Должны быть логи `Using API_URL: http://45.147.178.113:8080`
- Должны быть успешные запросы к `/api/students`, `/api/grades` и т.д.

## 🔧 Возможные проблемы

### CORS ошибки

Если видите ошибки вроде:
```
Access to fetch at 'http://45.147.178.113:8080/api/students' from origin 'https://yourname.github.io' has been blocked by CORS policy
```

**Решение**: Убедитесь что установлен и настроен aiohttp-cors (шаг 1-2)

### Connection refused

Если видите:
```
net::ERR_CONNECTION_REFUSED
```

**Решение**: 
- Проверьте что бот запущен на VDS
- Проверьте что порт 8080 открыт в файрволе
- Попробуйте `curl http://45.147.178.113:8080/api/students` с VDS

### Mixed Content (HTTP/HTTPS)

Если GitHub Pages использует HTTPS а VDS HTTP, браузер может блокировать запросы.

**Временное решение**: Используйте HTTP для GitHub Pages (не рекомендуется)

**Правильное решение**: Настройте HTTPS на VDS:
1. Получите домен
2. Установите Let's Encrypt сертификат
3. Используйте HTTPS

## 📝 Быстрая команда для настройки

### На локальной машине:

Выполните эти команды чтобы добавить все изменения:

```bash
# Добавить API_URL в index.html (вставьте перед </head>)
echo "Добавьте в index.html перед </head>:"
echo '    <script>const API_URL = "http://45.147.178.113:8080";</script>'

# Обновить api.js
echo "Обновите getBaseUrl() в api.js как показано выше"

# Коммит и пуш
git add .
git commit -m "Configure VDS API connection"
git push origin main
```

### На VDS:

```bash
# Установить зависимости
pip install aiohttp-cors

# Запустить бота
python bot_with_webapp.py
```

## ✅ После настройки

После всех шагов:
1. ✅ Mini App открывается с GitHub Pages
2. ✅ Запросы идут на VDS API (45.147.178.113:8080)
3. ✅ Данные загружаются из реальной базы данных
4. ✅ Можно добавлять/редактировать оценки
5. ✅ Все изменения сохраняются в БД

---

**Важно**: Это временное решение с HTTP. Для продакшена настройте HTTPS!
