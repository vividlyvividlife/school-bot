# 🛠 Инструкция по диагностике VDS

Похоже, что бот на сервере не запустился после обновления. Чтобы понять причину, нужно зайти на сервер и проверить логи.

## 1️⃣ Зайдите на сервер

Откройте терминал (PowerShell или CMD) и введите:

```bash
ssh root@45.147.178.113
```

*(Если попросит пароль — введите его)*

## 2️⃣ Проверьте логи

Выполните эту команду, чтобы увидеть последние ошибки:

```bash
tail -n 50 /botSchool/bot.log
```

И эту, чтобы увидеть, как прошел деплой:

```bash
tail -n 50 /botSchool/deploy.log
```

## 3️⃣ Попробуйте запустить вручную

Если в логах непонятно, попробуйте запустить бота вручную, чтобы увидеть ошибку прямо в консоли:

```bash
# 1. Остановите старого бота (если есть)
screen -X -S school_bot quit

# 2. Перейдите в папку
cd /botSchool

# 3. Активируйте окружение
source venv/bin/activate

# 4. Установите зависимости (на всякий случай)
pip install -r requirements.txt

# 5. Запустите бота
python bot_with_webapp.py
```

### Что должно произойти:
- Если всё хорошо, вы увидите:
  ```
  🤖 Bot started
  📱 Mini App available at http://0.0.0.0:8080
  ```
- Если есть ошибка (например, `ModuleNotFoundError` или `Address already in use`), она появится на экране. **Скопируйте её и пришлите мне.**

## 4️⃣ Если пишет "Address already in use"

Это значит, что старый процесс бота завис и занимает порт 8080. Нужно его убить:

```bash
# Найти процесс на порту 8080
lsof -i :8080

# Убить его (замените PID на число из команды выше)
kill -9 PID
```

Или просто убейте все процессы python:
```bash
pkill python
```

Затем попробуйте запустить снова (шаг 3).
