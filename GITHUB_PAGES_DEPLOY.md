# Деплой Mini App на GitHub Pages

## 🚀 Быстрый старт

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Название репозитория: `school-bot-webapp` (или любое другое)
3. Выберите **Public**
4. Нажмите **Create repository**

### Шаг 2: Загрузите файлы Mini App

**Вариант А: Через веб-интерфейс GitHub**

1. В созданном репозитории нажмите **Add file** → **Upload files**
2. Перетащите всю папку `webapp` (все файлы и подпапки)
3. Нажмите **Commit changes**

**Вариант Б: Через Git (рекомендуется)**

```bash
cd d:\chatbotSchool\webapp

# Инициализация Git
git init

# Добавление всех файлов
git add .

# Коммит
git commit -m "Initial commit: School Bot Mini App"

# Подключение к GitHub (замените YOUR_USERNAME и REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/school-bot-webapp.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

### Шаг 3: Включите GitHub Pages

1. В репозитории откройте **Settings** (⚙️)
2. В левом меню выберите **Pages**
3. В разделе **Source** выберите:
   - Branch: `main`
   - Folder: `/ (root)`
4. Нажмите **Save**
5. Подождите 1-2 минуты

✅ Ваш Mini App будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/school-bot-webapp/
```

### Шаг 4: Обновите URL в боте

1. Откройте `d:\chatbotSchool\keyboards.py`
2. Найдите строку:
```python
WEBAPP_URL = "https://YOUR_USERNAME.github.io/school-bot-webapp/"
```
3. Замените `YOUR_USERNAME` на ваш GitHub username
4. Сохраните файл

### Шаг 5: Перезапустите бота

```bash
python bot.py
```

### Шаг 6: Тестирование

1. Откройте бота в Telegram
2. Нажмите **🌐 Открыть таблицу**
3. Mini App должен открыться с вашим интерфейсом!

---

## 📝 Обновление Mini App

Когда вы изменили файлы в `webapp/`:

```bash
cd d:\chatbotSchool\webapp

git add .
git commit -m "Update: описание изменений"
git push
```

Изменения появятся на GitHub Pages через 1-2 минуты.

---

## 🔧 Настройка для продакшн

### Кастомный домен (опционально)

Если у вас есть свой домен:

1. В настройках GitHub Pages добавьте **Custom domain**
2. Создайте CNAME запись у регистратора домена:
   ```
   miniapp.yourdomain.com → YOUR_USERNAME.github.io
   ```
3. Обновите `WEBAPP_URL` в `keyboards.py`

### HTTPS

GitHub Pages автоматически предоставляет HTTPS ✅

---

## 📊 Структура для GitHub Pages

Ваша папка `webapp` должна содержать:

```
webapp/
├── index.html          # Главная страница
├── css/
│   └── style.css      # Стили
└── js/
    ├── api.js         # API helper
    ├── teacher.js     # Интерфейс учителя
    ├── parent.js      # Интерфейс родителя
    └── student.js     # Интерфейс ученика
```

Все эти файлы уже готовы и настроены! ✅

---

## ❓ FAQ

**Q: Нужно ли платить за GitHub Pages?**
A: Нет, это полностью бесплатно для публичных репозиториев.

**Q: Можно ли сделать репозиторий приватным?**
A: Да, но GitHub Pages для приватных репозиториев доступен только на платных планах.

**Q: Как часто можно обновлять?**
A: Без ограничений. Каждый push обновляет сайт.

**Q: Работает ли это с Telegram Mini App?**
A: Да! GitHub Pages предоставляет HTTPS, что требуется для Mini App.

**Q: Что делать, если Mini App не открывается?**
A: 
1. Проверьте, что URL в `keyboards.py` правильный
2. Убедитесь, что GitHub Pages активирован
3. Подождите 1-2 минуты после первого деплоя
4. Проверьте консоль браузера на ошибки

---

## 🎯 Альтернативы GitHub Pages

Если по какой-то причине GitHub Pages не подходит:

### Netlify
```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Деплой
cd d:\chatbotSchool\webapp
netlify deploy --prod
```

### Vercel
```bash
# Установите Vercel CLI
npm install -g vercel

# Деплой
cd d:\chatbotSchool\webapp
vercel --prod
```

### Cloudflare Pages
1. Зарегистрируйтесь на https://pages.cloudflare.com
2. Подключите GitHub репозиторий
3. Автоматический деплой при каждом push

---

## ✅ Готово!

После выполнения всех шагов:

- ✅ Mini App размещен на GitHub Pages
- ✅ Доступен по HTTPS
- ✅ Работает в Telegram
- ✅ Обновляется автоматически при push

**Наслаждайтесь вашим School Bot! 🎉**
