# Настройка автоматического деплоя через GitHub

Этот проект настроен для автоматического деплоя:
- **Бот** → VDS сервер
- **Mini App** → GitHub Pages

## 🚀 Автоматический деплой

### При каждом push в main:
1. ✅ Запускаются тесты
2. ✅ Бот деплоится на VDS (если изменились .py файлы)
3. ✅ Mini App деплоится на GitHub Pages (если изменились файлы в webapp/)

---

## 📋 Настройка VDS деплоя

### Шаг 1: Подготовка VDS сервера

```bash
# Подключитесь к VDS
ssh user@your-vds-ip

# Создайте директорию для проекта
sudo mkdir -p /botSchool
sudo chown $USER:$USER /botSchool

# Клонируйте репозиторий
cd /botSchool
git clone https://github.com/YOUR_USERNAME/school-bot.git .

# Создайте .env файл
nano .env
```

Добавьте в `.env`:
```
BOT_TOKEN=your_bot_token_here
WEBAPP_URL=https://YOUR_USERNAME.github.io/school-bot/
```

### Шаг 2: Первый запуск

```bash
# Установите зависимости
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Запустите бота
screen -dmS school_bot bash -c "source venv/bin/activate && python bot.py"

# Проверьте статус
screen -ls
```

### Шаг 3: Настройка SSH ключа для GitHub Actions

```bash
# На VDS сервере создайте SSH ключ
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions

# Добавьте публичный ключ в authorized_keys
cat ~/.ssh/github_actions.pub >> ~/.ssh/authorized_keys

# Скопируйте приватный ключ (понадобится для GitHub)
cat ~/.ssh/github_actions
```

### Шаг 4: Настройка GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты:

| Name | Value |
|------|-------|
| `VDS_HOST` | IP адрес вашего VDS (например: `123.45.67.89`) |
| `VDS_USERNAME` | Имя пользователя (например: `root` или `ubuntu`) |
| `VDS_SSH_KEY` | Приватный SSH ключ (содержимое `~/.ssh/github_actions`) |
| `VDS_PORT` | SSH порт (обычно `22`) |

### Шаг 5: Копирование скрипта деплоя на VDS

```bash
# На вашем компьютере
scp deploy_vds.sh user@your-vds-ip:/botSchool/

# На VDS сделайте скрипт исполняемым
ssh user@your-vds-ip
chmod +x /botSchool/deploy_vds.sh
```

---

## 🌐 Настройка GitHub Pages деплоя

### Шаг 1: Включите GitHub Pages

1. Откройте репозиторий на GitHub
2. **Settings** → **Pages**
3. В разделе **Build and deployment**:
   - Source: **GitHub Actions**
4. Сохраните

### Шаг 2: Обновите URL в keyboards.py

```python
# В файле keyboards.py
WEBAPP_URL = "https://YOUR_USERNAME.github.io/school-bot/"
```

Замените `YOUR_USERNAME` на ваш GitHub username.

---

## 🔄 Как это работает

### Workflow для VDS (`.github/workflows/deploy-vds.yml`)

Триггеры:
- Push в `main` ветку
- Изменения в `.py` файлах или `requirements.txt`
- Ручной запуск через GitHub Actions

Что делает:
1. Подключается к VDS по SSH
2. Останавливает текущую screen сессию
3. Делает `git pull` последних изменений
4. Обновляет зависимости
5. Запускает бота в новой screen сессии

### Workflow для GitHub Pages (`.github/workflows/deploy-pages.yml`)

Триггеры:
- Push в `main` ветку
- Изменения в `webapp/` директории
- Ручной запуск через GitHub Actions

Что делает:
1. Берет содержимое `webapp/` директории
2. Деплоит на GitHub Pages
3. Mini App доступен через 1-2 минуты

### Workflow для тестов (`.github/workflows/test.yml`)

Триггеры:
- Push в `main` или `develop`
- Pull Request в `main`

Что делает:
1. Запускает `test_db.py`
2. Проверяет код на ошибки (flake8)

---

## 📝 Использование

### Обычный workflow разработки:

```bash
# 1. Внесите изменения в код
nano bot.py

# 2. Закоммитьте и отправьте на GitHub
git add .
git commit -m "Update: описание изменений"
git push origin main

# 3. GitHub Actions автоматически:
#    - Запустит тесты
#    - Задеплоит бота на VDS
#    - Задеплоит Mini App на GitHub Pages (если изменились файлы webapp/)
```

### Просмотр логов деплоя:

1. Откройте репозиторий на GitHub
2. Вкладка **Actions**
3. Выберите последний workflow run
4. Посмотрите логи каждого шага

### Ручной деплой:

1. **Actions** → выберите workflow
2. **Run workflow** → **Run workflow**

---

## 🔍 Мониторинг и отладка

### Проверка статуса бота на VDS:

```bash
# Подключитесь к VDS
ssh user@your-vds-ip

# Посмотрите запущенные screen сессии
screen -ls

# Подключитесь к сессии бота
screen -r school_bot

# Отключитесь от сессии (не останавливая бота)
# Нажмите: Ctrl+A, затем D

# Посмотрите логи
tail -f /botSchool/deploy.log
tail -f /botSchool/bot.log
```

### Остановка бота:

```bash
# Остановить screen сессию
screen -X -S school_bot quit

# Или подключиться и нажать Ctrl+C
screen -r school_bot
# Ctrl+C
```

---

## 🛠️ Troubleshooting

### Деплой на VDS не работает

1. Проверьте SSH подключение:
   ```bash
   ssh -i ~/.ssh/github_actions user@your-vds-ip
   ```

2. Проверьте GitHub Secrets (правильные ли значения)

3. Посмотрите логи в GitHub Actions

### GitHub Pages не обновляется

1. Проверьте, что изменились файлы в `webapp/`
2. Посмотрите логи в GitHub Actions
3. Проверьте, что GitHub Pages включен в настройках

### Бот не запускается после деплоя

1. Проверьте логи:
   ```bash
   tail -f /botSchool/bot.log
   ```

2. Проверьте, что `.env` файл на месте:
   ```bash
   cat /botSchool/.env
   ```

3. Проверьте зависимости:
   ```bash
   cd /botSchool
   source venv/bin/activate
   pip list
   ```

---

## 📊 Структура проекта для CI/CD

```
school-bot/
├── .github/
│   └── workflows/
│       ├── deploy-vds.yml      # Деплой бота на VDS
│       ├── deploy-pages.yml    # Деплой Mini App на GitHub Pages
│       └── test.yml            # Автоматические тесты
├── webapp/                     # Mini App (деплоится на GitHub Pages)
├── bot.py                      # Основной файл бота
├── deploy_vds.sh              # Скрипт деплоя на VDS
├── requirements.txt
└── test_db.py                 # Тесты
```

---

## ✅ Готово!

После настройки:
- ✅ Каждый push автоматически деплоит изменения
- ✅ Тесты запускаются автоматически
- ✅ Бот всегда актуален на VDS
- ✅ Mini App всегда актуален на GitHub Pages

**Просто делайте `git push` и всё обновится автоматически! 🚀**
